apiVersion: v1
kind: Service

metadata:
  name: food-backend
  namespace: {{ .Release.Namespace }}

spec:
  type: ClusterIP

  selector:
    app: food-backend

  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP