apiVersion: v1
kind: Service

metadata:
  name: food-frontend
  namespace: {{ .Release.Namespace }}

spec:
  type: ClusterIP

  selector:
    app: food-frontend

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP